# Define a dictionary to map city names to their respective IDs
# This dictionary is used to filter hosts by city in the Zabbix API queries
# The keys are city names and the values are lists of group IDs associated with those cities
CITIES = dict(
    prudentopolis=[219],
    lapa=[161]
)
